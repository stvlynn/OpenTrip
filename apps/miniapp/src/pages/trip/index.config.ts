export default definePageConfig({
  // Custom bar mirrors the PWA planner header (back + title + agent toggle).
  navigationStyle: "custom",
  navigationBarTitleText: "OpenTrip",
  enablePullDownRefresh: true,
});
